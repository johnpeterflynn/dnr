{
    "name": "DNR",
    "n_gpu": 1,

    "arch": {
        "type": "RenderNet",
        "args": {
            "texture_size": 1024,
            "texture_depth": 16
        }
    },
    "data_loader": {
        "type": "UVDataLoader",
        "args":{
            "data_dir": "data/",
            "batch_size": 1,
            "skip": 6,
            "shuffle": true,
            "num_workers": 1
        }
    },
    "optimizer": {
        "type": "Adam",
        "args":{
            "lr": 0.001,
            "weight_decay": 0,
            "amsgrad": true
        }
    },
    "loss": "photometric_reproduction_loss",
    "metrics": [
	    "mse"
    ],
    "trainer": {
        "epochs": 200,

        "save_dir": "saved/",
        "save_period": 20,
        "verbosity": 2,
        
        "monitor": "min val_loss",
        "early_stop": 100,

        "tensorboard": true
    }
}
